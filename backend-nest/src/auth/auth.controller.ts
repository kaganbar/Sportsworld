import { Controller } from '@nestjs/common';

// Phase 2 skeleton only. Real routes (GET /api/auth/google,
// GET /api/auth/google/callback, POST /api/auth/refresh, GET /api/auth/me)
// land in Phase 4 alongside the Passport strategies/guards this depends on.
@Controller('auth')
export class AuthController {}
